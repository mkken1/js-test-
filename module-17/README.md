# Модуль 17: HTML основы

## 🎯 Цель модуля

Изучить основы HTML — языка разметки веб-страниц.

---

## 📚 Теория

### Что такое HTML?

**HTML** (HyperText Markup Language) — язык разметки, который определяет структуру веб-страницы. HTML использует теги для описания элементов страницы.

### Структура HTML-документа

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Заголовок страницы</title>
</head>
<body>
  <!-- Содержимое страницы -->
</body>
</html>
```

| Элемент | Назначение |
|---------|------------|
| `<!DOCTYPE html>` | Объявление типа документа (HTML5) |
| `<html>` | Корневой элемент |
| `<head>` | Метаданные (не отображаются) |
| `<body>` | Видимое содержимое страницы |

### Основные теги

#### Заголовки (h1-h6)

```html
<h1>Главный заголовок</h1>
<h2>Подзаголовок</h2>
<h3>Заголовок третьего уровня</h3>
```

#### Текст

```html
<p>Это параграф текста.</p>
<strong>Жирный текст</strong>
<em>Курсив</em>
<br> <!-- Перенос строки -->
```

#### Ссылки

```html
<a href="https://example.com">Текст ссылки</a>
<a href="/about">Внутренняя ссылка</a>
<a href="#section">Якорная ссылка</a>
```

#### Изображения

```html
<img src="photo.jpg" alt="Описание изображения">
<img src="https://example.com/image.png" alt="Внешнее изображение" width="300">
```

#### Списки

```html
<!-- Маркированный список -->
<ul>
  <li>Пункт 1</li>
  <li>Пункт 2</li>
</ul>

<!-- Нумерованный список -->
<ol>
  <li>Первый</li>
  <li>Второй</li>
</ol>
```

#### Контейнеры

```html
<div>Блочный контейнер (занимает всю ширину)</div>
<span>Строчный контейнер (внутри текста)</span>
```

### Атрибуты

Атрибуты добавляют дополнительную информацию к элементам:

```html
<a href="url" target="_blank" title="Подсказка">Ссылка</a>
<img src="photo.jpg" alt="Описание" width="200" height="100">
<div id="header" class="container">Контент</div>
```

| Атрибут | Назначение |
|---------|------------|
| `id` | Уникальный идентификатор |
| `class` | Класс для стилизации |
| `style` | Inline-стили |
| `title` | Всплывающая подсказка |

### Формы

```html
<form action="/submit" method="POST">
  <label for="name">Имя:</label>
  <input type="text" id="name" name="name" placeholder="Введите имя">

  <label for="email">Email:</label>
  <input type="email" id="email" name="email">

  <label for="password">Пароль:</label>
  <input type="password" id="password" name="password">

  <button type="submit">Отправить</button>
</form>
```

#### Типы input

| Тип | Описание |
|-----|----------|
| `text` | Текстовое поле |
| `password` | Поле для пароля |
| `email` | Email с валидацией |
| `number` | Числовое поле |
| `checkbox` | Чекбокс |
| `radio` | Радиокнопка |
| `submit` | Кнопка отправки |

### Семантические теги HTML5

```html
<header>Шапка сайта</header>
<nav>Навигация</nav>
<main>Основной контент</main>
<article>Статья</article>
<section>Секция</section>
<aside>Боковая панель</aside>
<footer>Подвал сайта</footer>
```

---

## 💡 Примеры

### Пример 1: Простая страница

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Моя первая страница</title>
</head>
<body>
  <h1>Привет, мир!</h1>
  <p>Это мой первый HTML-документ.</p>
</body>
</html>
```

### Пример 2: Страница с навигацией

```html
<header>
  <h1>Мой сайт</h1>
  <nav>
    <a href="/">Главная</a>
    <a href="/about">О нас</a>
    <a href="/contact">Контакты</a>
  </nav>
</header>

<main>
  <article>
    <h2>Заголовок статьи</h2>
    <p>Текст статьи...</p>
  </article>
</main>

<footer>
  <p>&copy; 2024 Мой сайт</p>
</footer>
```

### Пример 3: Форма регистрации

```html
<form>
  <h2>Регистрация</h2>

  <div>
    <label for="username">Логин:</label>
    <input type="text" id="username" name="username" required>
  </div>

  <div>
    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required>
  </div>

  <div>
    <label for="pwd">Пароль:</label>
    <input type="password" id="pwd" name="password" minlength="6" required>
  </div>

  <button type="submit">Зарегистрироваться</button>
</form>
```

---

## ✏️ Задание

### Описание

Напишите функцию `createHTML`, которая генерирует HTML-разметку карточки пользователя.

### Требования

1. Функция принимает объект пользователя: `{ name, email, avatar }`
2. Возвращает строку с HTML-разметкой
3. Структура карточки:
   - Контейнер `<div class="user-card">`
   - Изображение `<img>` с src из avatar и alt с именем
   - Заголовок `<h2>` с именем
   - Параграф `<p>` с email

### Пример результата

```javascript
createHTML({ name: 'Иван', email: 'ivan@mail.ru', avatar: 'photo.jpg' })
```

Должен вернуть:
```html
<div class="user-card">
  <img src="photo.jpg" alt="Иван">
  <h2>Иван</h2>
  <p>ivan@mail.ru</p>
</div>
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>
Какой синтаксис в JavaScript позволяет удобно вставлять значения переменных прямо внутрь строки? Подумайте о шаблонных строках.
</details>

<details>
<summary>Подсказка 2: Структура</summary>

Функция должна возвращать одну строку, содержащую HTML-разметку. Самый удобный способ собрать строку с подставленными значениями — шаблонные литералы (обратные кавычки), где через `${выражение}` можно вставить любое свойство объекта прямо внутрь строки.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Создайте функцию, которая принимает объект пользователя и возвращает шаблонный литерал с HTML-разметкой. Внутри литерала постройте структуру: корневой `div` с классом `user-card`, внутри него `img` (используйте свойства `avatar` для `src` и `name` для `alt`), затем `h2` с именем и `p` с email. Подставляйте свойства объекта через `${}`. Обратите внимание на точные отступы — тесты могут проверять формат строки.

</details>

---

## 🧪 Как проверить решение

```bash
npx vitest module-17/index.spec.js
```

---

## 📖 Дополнительные материалы

- [MDN: HTML](https://developer.mozilla.org/ru/docs/Web/HTML)
- [HTML Reference](https://htmlreference.io/)
- [HTML Validator](https://validator.w3.org/)

---

## ❓ Частые вопросы

**Q: В чём разница между div и span?**

A: `div` — блочный элемент (занимает всю ширину, начинается с новой строки). `span` — строчный (только по ширине содержимого, внутри текста).

**Q: Обязательно ли закрывать теги?**

A: В HTML5 некоторые теги можно не закрывать (`<br>`, `<img>`, `<input>`), но для читаемости лучше использовать самозакрывающиеся теги или явное закрытие.

**Q: Что такое семантика в HTML?**

A: Использование тегов по назначению: `<nav>` для навигации, `<article>` для статей, вместо `<div>` везде. Это улучшает SEO и доступность.

---

## 🎓 Что дальше?

После выполнения переходите к **Модуль 18: DOM** — научитесь работать с HTML из JavaScript.
